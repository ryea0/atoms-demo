# 依赖与构建产物（镜像内重新安装/构建，防宿主平台二进制污染镜像）
node_modules
node_modules/
.next
out
build
coverage

# 运行数据与密钥（永不进镜像）
data
*.db
*.db-wal
*.db-shm
.env*
!.env.example

# 版本控制与本地工作区
.git
.gitignore
.superpowers

# 测试与文档（运行时不需要）
tests
docs

# 注：src/lib/agents/roles/samples/ 不可忽略——mock provider 运行时按文本读取这些样例
